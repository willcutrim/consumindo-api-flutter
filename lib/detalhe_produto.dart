import 'package:flutter/material.dart';

class DetalheProdutoPage extends StatelessWidget {
  const DetalheProdutoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
